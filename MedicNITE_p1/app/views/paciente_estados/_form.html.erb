<%= form_for(@paciente_estado) do |f| %>
  <% if @paciente_estado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@paciente_estado.errors.count, "error") %> prohibited this paciente_estado from being saved:</h2>

      <ul>
      <% @paciente_estado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :idEstadoPaciente %><br>
    <%= f.number_field :idEstadoPaciente %>
  </div>
  <div class="field">
    <%= f.label :estadoPaciente %><br>
    <%= f.text_field :estadoPaciente %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
