<%= form_for @paciente_estado, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :idEstadoPaciente, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :idEstadoPaciente, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :estadoPaciente, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :estadoPaciente, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                paciente_estados_path, :class => 'btn' %>
  </div>
<% end %>
